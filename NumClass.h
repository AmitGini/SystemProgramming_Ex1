#ifndef _NUMCLASS_H_
#define _NUMCLASS_H_

    //will return if a number is Armstrong number.
    int isArmstrong(int x);

    //will return if the number is Palindrome number.
    int isPalindrome(int x);

    //will return if the number Prime number.
    int isPrime(int x);

    //will return if the number is Strong number.
    int isStrong(int x);
    
#endif